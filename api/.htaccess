RewriteEngine On

# Clean URL: /payment/webhook → webhook.php
RewriteRule ^payment/webhook$ webhook.php [L,QSA]

# Block everything else from browsing /api/
RewriteCond %{REQUEST_URI} !^/api/webhook\.php$
RewriteCond %{REQUEST_URI} !^/api/payment/webhook$
RewriteRule ^ - [F,L]
