RewriteEngine On

# Clean URL: /payment/webhook → webhook.php
RewriteRule ^payment/webhook$ webhook.php [L,QSA]

# Block direct access to webhook.php itself (must go through clean URL)
RewriteCond %{THE_REQUEST} ^[A-Z]+\s/webhook\.php [NC]
RewriteRule ^ - [F,L]
